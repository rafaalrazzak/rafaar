---
import { cn } from '@/libs/utils';

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class={cn('mx-auto w-full max-w-6xl px-4 py-20 sm:px-6 lg:px-8', className)}>
  <slot />
</div>

