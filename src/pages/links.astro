---
import MainLayout from '@/layouts/MainLayout.astro';
import { RESUME_DATA } from '@/data/resume-data';
import { projects } from '@/data/links/projects';
import SectionTitle from '@/components/ui/SectionTitle.astro';
import InlineLink from '@/components/ui/InlineLink.astro';
import PageShell from '@/components/layout/PageShell.astro';
import PageStack from '@/components/layout/PageStack.astro';
import PageHero from '@/components/layout/PageHero.astro';
import SectionStack from '@/components/layout/SectionStack.astro';
import CardStack from '@/components/layout/CardStack.astro';
import PageHeading from '@/components/typography/PageHeading.astro';
import PageLead from '@/components/typography/PageLead.astro';
---

<MainLayout title="Links">
  <PageShell>
    <PageStack>
      <!-- Header -->
      <PageHero>
        <div class="space-y-6">
          <PageHeading>Links</PageHeading>
          <PageLead>
            Connect with me across the web.
          </PageLead>
        </div>

        <div class="flex flex-wrap gap-x-6 gap-y-3 text-sm">
          {
            RESUME_DATA.contact.social.map((social) => (
              <InlineLink
                href={social.url}
                target="_blank"
                rel="noopener noreferrer"
              >
                <social.icon className="text-muted-foreground hover:text-foreground size-4 fill-current transition-colors" />
                <span>{social.name}</span>
              </InlineLink>
            ))
          }
        </div>
      </PageHero>

      <!-- Projects -->
      <SectionStack>
        <SectionTitle>Projects</SectionTitle>
        <CardStack>
          {
            projects.map((project) => (
              <a
                href={project.url}
                target="_blank"
                rel="noopener noreferrer"
                class="group flex items-start gap-4"
              >
                <project.icon className="text-muted-foreground group-hover:text-foreground mt-0.5 size-5 shrink-0 transition-colors" />
                <div class="flex-1 space-y-1">
                  <h3 class="text-foreground font-medium">{project.title}</h3>
                  <p class="text-muted-foreground text-sm">
                    {project.description}
                  </p>
                </div>
              </a>
            ))
          }
        </CardStack>
      </SectionStack>
    </PageStack>
  </PageShell>
</MainLayout>
